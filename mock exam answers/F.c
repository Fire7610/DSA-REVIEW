#include <stdio.h>

int in(char a){
    if (a = 's')
        return 1;
    else return 2;
}

int main(){
    int tc = 4,ctr = 0;
    char input[100] = {'V','I','S','A','Y','A','S'}, temp;

    do{
        char inp;
        scanf(" %c", &inp);
        
        switch (in(inp))
        {
            case 1:
                temp = ';';
                
        
        }

        
    }while(ctr<tc);
}